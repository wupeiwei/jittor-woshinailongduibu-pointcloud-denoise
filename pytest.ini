[pytest]
testpaths = tests
addopts = -p no:cacheprovider
markers =
    gpu: marks tests requiring GPU (deselect with '-m "not gpu"')
    submission: marks submission zip validation tests
    registry: marks candidate registry consistency tests
    leak_check: marks inference logic leak-detection tests
