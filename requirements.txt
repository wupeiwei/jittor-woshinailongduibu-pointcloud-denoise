# Formal point-cloud denoising project dependencies.
# Python: recommended 3.10-3.12.
# Install in a venv/conda env, not into system Python.

jittor>=1.3.9
# Jittor CUDA sync/backward may import CuPy on CUDA 12.x machines.
# Pin to CuPy 13.x because CuPy 14.x pulls numpy>=2, while this project keeps numpy<2.
# For CUDA 11.x, install the matching cupy-cuda11x wheel manually if needed.
cupy-cuda12x>=13.0,<14.0
numpy>=1.23,<2.0
trimesh>=4.0
PyYAML>=6.0
scipy>=1.10
omegaconf>=2.3

tqdm>=4.60
